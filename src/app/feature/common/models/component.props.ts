// Interface para el componente BackButton
export interface BackButtonProps {
  onPress: () => void;
  disablePersonalization?: boolean;
}

// Interface para el componente ScreenTitle
export interface ScreenTitleProps {
  text: string;
}
